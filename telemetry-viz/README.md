# N5 MAX · M.2 telemetry viz

Stylized motherboard schematic + 1 Hz playback.

- **Calibration** (default): Protocol B `stage1-raidz2-calibrate` pack under `data/calibration/` — **CALIBRATE · provisional · shape/DEMO only**, not a story promote.
- **DEMO**: synthetic playlist fallback (`js/demoTelemetry.js`).

Column map: `ADAPTER.md`.
