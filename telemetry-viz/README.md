# N5 MAX · M.2 telemetry viz

Stylized motherboard schematic + 1 Hz playback.

- **Calibration** (default): sealed Protocol B `stage1-raidz2-calibrate` pack under `data/calibration/` — **shape/DEMO only**, not a story promote.
- **DEMO**: synthetic playlist fallback (`js/demoTelemetry.js`).

Schema: lab `docs/result-schema.md`. Column map: `ADAPTER.md`. Rebuild pack: `scripts/build-calibration-pack.py`.
